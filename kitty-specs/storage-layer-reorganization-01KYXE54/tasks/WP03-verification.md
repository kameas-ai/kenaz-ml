---
work_package_id: WP03
title: Verification and Documentation
dependencies:
- WP02
requirement_refs:
- FR-006
- FR-007
- NFR-001
- NFR-003
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
subtasks:
- T014
- T015
- T016
- T017
agent: "claude"
shell_pid: "30290"
history:
- date: '2026-08-01'
  note: Generated by /spec-kitty.tasks
authoritative_surface: tests/test_storage_reorganization.py
execution_mode: code_change
owned_files:
- CLAUDE.md
- docs/ML_ARCHITECTURE.md
- tests/test_storage_reorganization.py
tags: []
---

# WP03 — Verification and Documentation

## Objective

Prove the move changed nothing, and update the documentation that names the old paths.

## Context

WP02 moved the code and the suite passed. That establishes the *tests* still pass — not that a shipped artifact still loads, that the frozen binary still builds, or that nothing survives at an old path by accident.

`CLAUDE.md` names `src/sigil_ml/store.py` directly, so it is wrong the moment WP02 lands. `docs/ML_ARCHITECTURE.md` references it too.

Read first: [quickstart.md](../quickstart.md), [spec.md](../spec.md) FR-004/006/007/010, and `occurrence_map.yaml`.

## Branch Strategy

```bash
spec-kitty agent action implement WP03 --agent <name> --mission storage-layer-reorganization-01KYXE54
```

**Depends on WP02.**

---

### T014 — Old paths gone, public surface resolves

**Purpose**: FR-004 and FR-010, asserted rather than assumed.

**Steps**:

1. Create `tests/test_storage_reorganization.py`.
2. Assert every old path raises `ModuleNotFoundError`:
   `sigil_ml.store`, `sigil_ml.store_sqlite`, `sigil_ml.store_postgres`, `sigil_ml.storage.model_store`, `sigil_ml.storage`, `sigil_ml.loader`, `sigil_ml.cache`.
3. Assert every name in the plan's public surface imports **from the package**, not merely from a submodule — the registry mission depends on that surface.
4. Assert `__all__` is declared in both packages and matches what is importable.

**Files**: `tests/test_storage_reorganization.py`

**Validation**:
- [ ] All seven old paths assert `ModuleNotFoundError`
- [ ] Every public name resolves from the package
- [ ] `__all__` matches the importable surface

---

### T015 — The frozen binary builds and serves

**Purpose**: FR-006.

**Steps**:

1. Build: `make freeze` (or the project's freeze target).
2. `collect_submodules("sigil_ml")` should reach the new subpackages without a spec change — **verify, do not assume**. If the spec needs a change, that is a finding worth reporting since it means the collector was relying on the flat layout.
3. **Exercise a real prediction in the built binary.** A clean build proves very little: the Feast mission's WP05 found two packaging bugs that appeared only on first *use*, both of the shape "builds clean, dies on first call".
4. If the build is not possible in this environment, say so plainly and report exactly how far you got. A partial honest result beats an untested claim.

**Files**: verification only; report findings rather than editing `freeze/` (WP02 owns it)

**Validation**:
- [ ] Binary builds, or the blocker is reported precisely
- [ ] A real prediction exercised, not just startup
- [ ] Any freeze-spec change reported as a finding

---

### T016 — Pre-change artifacts load; import time holds

**Purpose**: FR-007 and NFR-003.

**Steps**:

1. Load the `.joblib` fixture WP02's Part 1 recorded before the move and assert the model is equivalent — same type, same predictions on a fixed input. Nothing about the artifact format was meant to change, so a failure means something moved that should not have.
2. Measure `python -X importtime -c "import sigil_ml.app"` and compare against WP02's recorded baseline. Budget is 10% (NFR-003). Two new `__init__.py` files doing re-exports could plausibly pull more at import time than the flat modules did.
3. If import time regressed past budget, report it with the number — lazy re-exports are the fix, but that is a decision, not something to slip in here.

**Files**: `tests/test_storage_reorganization.py`

**Validation**:
- [ ] Pre-change artifact loads and predicts identically
- [ ] Import time measured against baseline and reported
- [ ] Any regression reported with numbers, not silently absorbed

---

### T017 — Documentation and occurrence-map cross-check

**Purpose**: FR-010, NFR-004.

**Steps**:

1. Update `CLAUDE.md` — it names `src/sigil_ml/store.py` in the `DataStore` invariant. Point it at `sigil_ml.datastore`, and note where model-artifact concerns now live, since the registry mission will land there.
2. Update `docs/ML_ARCHITECTURE.md` where it references moved paths.
3. **Cross-check `occurrence_map.yaml` against the final diff.** Every listed occurrence should be accounted for, and the diff should contain no moved-symbol edit the map did not anticipate. An unlisted edit means the map was incomplete — record that, it is the most useful output for the next bulk edit.
4. Record the reconciliation in your report.

**Files**: `CLAUDE.md`, `docs/ML_ARCHITECTURE.md`

**Validation**:
- [ ] `CLAUDE.md` invariant 5 points at the new path
- [ ] `ML_ARCHITECTURE.md` references updated
- [ ] Map cross-checked against the diff both ways
- [ ] Any map gap recorded

---

## Definition of Done

- [ ] All four subtasks complete
- [ ] Old paths provably gone; public surface provably resolves
- [ ] Frozen binary builds and serves, or the blocker is precisely reported
- [ ] Pre-change artifact loads unchanged
- [ ] Import time within 10%, measured
- [ ] Docs corrected
- [ ] `pytest tests/` passes; `ruff` clean

## Risks

| Risk | Mitigation |
|---|---|
| A clean freeze build accepted as proof | T015 requires exercising a real prediction |
| Docs left naming old paths, misleading the next agent | T017 owns both files |
| Import-time regression absorbed silently | T016 requires the number, and escalation past budget |
| Map gaps go unrecorded, so the next bulk edit repeats them | T017 requires a two-way cross-check |

## Reviewer Guidance

1. **Check T015 exercised a real feature call**, not just startup — that is the failure shape this codebase has already produced once.
2. **Check `CLAUDE.md` invariant 5** now names the correct module; it is the first thing the next agent reads.
3. **Read the map cross-check.** A diff containing moved-symbol edits the map never listed means the guardrail did not work, which matters beyond this mission.

## Activity Log

- 2026-08-02T00:16:57Z – claude – shell_pid=30290 – Started implementation via action command
